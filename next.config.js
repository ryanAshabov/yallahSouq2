/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'via.placeholder.com', 'ivccvshyhuwpuedzltht.supabase.co'],
    unoptimized: true,
  },
}

module.exports = nextConfig
